# CrewWord
CrewWord is a lightweight word processor built with Python and Tkinter. It uses the custom `.cwd` file format and currently supports creating, editing, opening, and saving documents. More features like rich text, images, tables, and a ribbon interface are planned.
